name: MyFirstWorkflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome Page
        run: echo "My first GitHub Actions job"
      - name: List Files
        run: ls


